---
import ModulePlaceholder from "../_shared/ModulePlaceholder.astro";
import type { Locale } from "../../i18n";

interface Props {
  locale: Locale;
}
const { locale } = Astro.props;
---

<ModulePlaceholder locale={locale} id="menu" />
