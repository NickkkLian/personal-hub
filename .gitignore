# build output
dist/
.astro/

# dependencies
node_modules/

# environment / secrets — never commit tokens
.env
.env.*

# sync script output (generated, P1+)
src/data/*.normalized.json

# 误放在本仓库目录内的其它项目/私有数据目录——绝不入库（personal-hub 是公开仓库）
/Database/
/PIP/
/hub-apps/
/personal-hub-admin/

# os / editor
.DS_Store
*.log

# claude code personal local settings (skills/ 等项目级配置仍纳入版本控制)
.claude/settings.local.json
